# Organisation Master Plus Value

- Ce dossier contient les setups qui ont sorti une plus-value positive.
- Garder pour chaque setup:
  - date/heure du cycle
  - tag du cycle
  - PnL total
  - description cycle conservee

## Regle de promotion

1. Setup positif -> archive dans `master_plus_value/`.
2. Setup revalide sur cycles supplementaires -> promotion dans `master_base/models/`.
