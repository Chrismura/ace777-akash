# Copier vers ~/.mexc.env (hors repo) — ne jamais committer de vraies clés.
# chmod 600 ~/.mexc.env
#
# Public ticker/depth/klines marchent SANS clés.
# Clés = plus tard (ordres live, compte).

MEXC_API_KEY=
MEXC_API_SECRET=

# Optionnel : base URL
MEXC_BASE_URL=https://api.mexc.com
