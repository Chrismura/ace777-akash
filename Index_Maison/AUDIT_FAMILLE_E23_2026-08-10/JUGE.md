# AUDIT JUGE (task signets.juge) — E2+E3

provider: OpenRouter Juge (nemotron-3-super-120b free) - verificateur independant

**Audit E2 + E3 – JUGE ACE777**

| Point | Analyse | Réserve éventuelle |
|-------|---------|--------------------|
| **1. Plists (120 s / 1800 s)** | - `state-generator.plist` : `StartInterval 120` → état toutes les 2 min (respecte la réserve P2 latence).<br>- `backup-check.plist` : `StartInterval 1800` → présence toutes les 30 min (réserve P5 I/O légère).<br>- Les deux plists déclarent `LowPriorityIO true` et `ProcessType Background`, ce qui limite l’impact sur un Mac 8 Go. | Aucun risque majeur détecté. Surveillance recommandée de l’usage disque lors du run des tailles (toutes les 6 h) mais dans les limites prévues. |
| **2. backup_light_check.sh** | - Présence : métadonnées uniquement (`test -d`) à chaque exécution (30 min) → I/O négligeable.<br>- Taille : calcul du `-sk` uniquement si le fichier de timestamp `.backup_size_ts` dépasse 6 h (`SIX_HOURS=21600`).<br>- Écrit deux fichiers bruts (`backup_presence.json`, `backup_sizes.json`) puis met à jour le timestamp. L’implémentation du cache est correcte et empêche les lectures disque lourdes plus souvent que prévu. | L’appel `du -sk` reste une opération I/O potentiellement lourde, mais limité à une exécution toutes les 6 h → acceptable sur la machine cible. Prévoir une surveillance ponctuelle si la taille des dossiers augmente fortement. |
| **3. _check_rust_version** | - Fonction lit `VERSION` dans le répertoire Rust core.<br>- Gère trois cas : fichier manquant → `ERROR` (non‑fatale), version différente → `WARNING`, version OK → `INFO`.<br>- Tout est enveloppé dans un `try/except` qui retourne silencieusement en cas d’exception, garantissant **non‑fatalité**.<br>- Conforme exactement à la réserve P4 (vérification de version, warning/error, ne plante jamais le script). | Aucune réserve. |
| **4. README_MIROFISH.md** | - Décision familiale (option c) clairement documentée : Mirofish en pause budgétaire, désactivé + retiré de la liste surveillée.<br>- Procédure de ré‑activation : <br> 1️⃣ Restaurer les plists depuis `DESACTIVES_2026-08-10/` → `~/Library/LaunchAgents/`.<br> 2️⃣ Supprimer la règle `skip_check` dans le superviseur (section indiquée).<br>- Le texte précise que la ré‑activation nécessite une décision familiale (COLLECTIVEMENT) et que le code reste conservé en backup uniquement. | La procédure omet la étape de rechargement des plists (`launchctl load …`) après la copie, bien que cela soit généralement implicite pour un utilisateur averti. Ajouter une note explicite éviterait toute ambiguïté. |
| **5. Verdict final E2+E3** | Toutes les exigences spécifiées (E2 : plists corrects, backup léger ; E3 : réversibilité documentée, vérification Rust non‑fatale) sont satisfaites. Les seules réserves mineures concernent la surveillance ponctuelle de l’I/O du `du -sk` toutes les 6 h et l’ajout d’une instruction de rechargement des plists dans le README. | **GO AVEC RESERVES** – accepter l’implémentation actuelle, en réservant : <br>• Surveiller l’impact I/O du contrôle de taille toutes les 6 h (mais il reste dans les limites prévues).<br>• Compléter le README_MIROFISH.md avec la commande `launchctl load` (ou équivalent) pour garantir une ré‑activation immédiate après restauration des plists. | 

**Conclusion** : L’implémentation E2 + E3 est conforme aux spécifications V2.1 et aux réserves familiales. Avec les réserves ci‑dessus notées, le système peut être considéré comme prêt pour la production sur le Mac 8 Go.
