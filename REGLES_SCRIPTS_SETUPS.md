# RÈGLE DE BASE — Scripts et Setups

**À appliquer systématiquement :**

1. **Affichage complet** : Quand l'utilisateur demande un script, un setup ou une configuration, **toujours afficher le contenu complet en entier** pour qu'il puisse l'analyser avant de l'exécuter ou de le coller dans le terminal. Pas de raccourcis.

2. **Constantes de temps (ORDRE IMPÉRATIF)** : Interdiction de modifier. Dans tout `requests.get()` pour Binance/ACE777 : `timeout=0.2` (ou max 0.3) et `verify=False`. Sans discussion.
