
## 31/08 — FIX refresh cockpit en boucle (Buffy)
Symptôme : la page se rechargeait sans arrêt. Cause : le feed tamponnait la meta version avec un epoch à CHAQUE run → l'auto-éjection du cockpit (reload si le code change) tirait dès que le feed tournait, même sans changement de code.
Fix (cockpit_mission_feed.py) : la meta version = hash SHA256 du contenu du cockpit (hors version et ?v=) → ne change QUE si le code est édité. Le ?v= reste epoch (fraîcheur des données mission.js, ne déclenche pas de reload). Vérifié : 2 runs successifs → version stable ; édition du code → version change (reload). Version actuelle cockpit : 3003108738.

## 31/08 — SHORT BTC PAPER implémenté (GO Christophe)
Audit du jour : AUCUN actif long ne contre BTC (portefeuille 0% opposés/72h + top 180 Binance tout corrélé). La seule contre-tendance = short BTC sur signal de sommet. Implémenté en PAPER : scripts/short_btc.py (zéro appel réseau, lit croisement_contexte.jsonl), score /10 (contexte sommet divergence + surchauffe %paires m6>3% + m6 BTC + move24), entrée ≥5 session 08-17h UTC, sortie TP -2%/SL +1.5%/signal éteint/24h. Taille 5$ (1/4 base), capital virtuel 100$. launchd com.ace777.short-btc (5 min). Cockpit : carte SHORT BTC onglet STRATÉGIE via pont /shortbtc. Audit complet dans short_btc_signaux.jsonl. 1re position ouverte @ 78690 (score 5). Protocole de validation : docs/SPEC_SHORT_BTC_20260831.md (3 jours de stabilité avant tout passage réel — jamais sans GO).

## 31/08 — Consultation famille+codeur « moteur léger et costaud » (7/7)
Soumis le diagnostic chiffré (Hulk ~200-270 appels/min vs limite MEXC 200, monolithe) + plan en 5 étapes à la famille (6 membres) et au codeur. Consensus unanime : (1) batch prix = immédiat, (4) cœur/satellites = la voie structurelle, (5) circuit-breaker 429 = critique. Corrections imposées : mon timeout 10s×2 contesté (faux positifs sur Wi-Fi) → 15s×1 retry + backoff sur 429/5xx + fallback dernier prix ; cache prix 15-20s piégé → 1 appel batch par cycle figé. Contribution majeure famille : écriture atomique des JSON (os.replace) + staleness check + WebSocket à terme + rate-limiter + RAM disk. Synthèse dans CONSULTATION_FAMILLE_CODEUR_MOTEUR_LEGER_20260831/SYNTHESE.md. Plan d'exécution : Phase1 batch+timeout+atomique+drift ; Phase2 espacer+sondes en satellites ; Phase3 WebSocket. GO Christophe attendu pour Phase 1.

## 31/08 — Phase 1 « moteur léger et costaud » APPLIQUÉE (batch prix)
Famille 7/7 + codeur ont validé le plan et corrigé le mien (timeout 10s→15s, cache piégé). Patch appliqué dans paper_diprip.py : (1) BATCH PRIX — 1 appel /ticker/price au début de chaque cycle → cache _PRICE_CACHE, last_price lit le cache (0 appel réseau en régime), fallback GET unitaire si paire absente, dernier prix connu en ultime recours ; (2) http_json durci — timeout 40→15s, retries 4→2, backoff exponentiel UNIQUEMENT sur 429/5xx ; (3) anti-drift de boucle (sleep jusqu'au prochain tick absolu). Logique métier inchangée (16 positions reprises en --resume). Moteur redémarré proprement (STOP_PAPER → watchdog --resume), PID 8542, heartbeat open=16 continu, zéro erreur réseau. save_state avait déjà l'écriture atomique (24/08). Reste Phase 2 : espacer klines/carnet (book_sense reste le coût résiduel) + sondes en satellites.

## 31/08 — Phase 2 « moteur léger » APPLIQUÉE (carnet cache + régimes espacés)
Suite de la Phase 1 (batch prix). Phase 2 = alléger le coût résiduel identifié : book_sense (carnet depth = 1 appel/paire/cycle, le gros résiduel) passé en CACHE TTL 45s (SENSE_CACHE_TTL_SEC) → réutilisé au lieu de relire le depth de chaque paire à chaque tick (rotation famille) ; refresh_scores (klines 360 bougies × paires = ~42 appels) espacé de tous les 3 cycles (~1 min) à tous les 10 (~3 min) via SCORE_EVERY=10. Prix toujours frais via le batch Phase 1. Gain combiné estimé : ~200-270 req/min → ~35 req/min. Testé (cache carnet 3/3), moteur redémarré proprement (STOP_PAPER → watchdog --resume), PID 10364, 16 positions reprises, heartbeat open=16 continu, CPU ~0%, RSS 20MB, zéro erreur réseau. Reste : sortie des sondes (aspiration, GEX) en satellites = chantier structurel (Phase 3).
