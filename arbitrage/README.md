# Arbitrage (Backlog ACE777)

Ce dossier est reserve a la couche "arbitrage volume" d'ACE777.

Objectif:
- Preparer une execution gros volume coherente (sans casser la protection actuelle).
- Revenir ici quand le mode actuel sera stable en run live/testnet.

Piste v1 a implementer plus tard:
- Execution TWAP/VWAP (decoupage en sous-ordres).
- Limite de participation au carnet (impact controle).
- Garde slippage par tranche + abort si spread degrade.
- Mesure du cout d'execution (bps vs prix de reference).

Etat:
- En attente (ne pas activer pendant les runs de calibration actuels).
